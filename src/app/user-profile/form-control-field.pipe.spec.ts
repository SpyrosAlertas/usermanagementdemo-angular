import { FormControlFieldPipe } from './form-control-field.pipe';

describe('FormControlFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
